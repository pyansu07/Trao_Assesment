# Base URL of the backend API (see backend/.env.example BACKEND_URL).
# Must keep the NEXT_PUBLIC_ prefix - the browser calls this API directly,
# so Next.js needs to inline it into the client bundle at build time.
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
